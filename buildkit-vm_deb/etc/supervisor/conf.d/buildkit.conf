; ============
; buildkit VMS 
; ============

; Note: Remember to give each command a different tunnel, eg qtap0, qtap1 etc


; [program:buildkit10]
; command=/usr/bin/buildkit-vm-start eth0 qtap0 1024M 4 /var/lib/buildkit/vm/buildkit10/disk.raw -nographic
; user=root
; numprocs=1
; stdout_logfile=/var/log/buildkit-vm-buildkit10.out.log
; stderr_logfile=/var/log/buildkit-vm-buildkit10.err.log
; autostart=true
; autorestart=true
; startsecs=10

